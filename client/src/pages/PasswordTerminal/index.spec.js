import * as React from 'react'
import { render } from '@testing-library/react'
import PasswordTerminal from '.'

describe('PasswordTerminal', () => {
  it.todo('should render component correctly')
})
